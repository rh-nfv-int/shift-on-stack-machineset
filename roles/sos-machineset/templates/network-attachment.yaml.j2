apiVersion: k8s.cni.cncf.io/v1
kind: NetworkAttachmentDefinition
metadata:
  name: "{{ net_name }}"
spec:
  config: |-
    {
      "cniVersion": "0.3.1",
      "name": "{{ net_name }}",
      "type": "host-device",
      "pciBusId": "{{ pci_address}}",
      "ipam": { }
    }
