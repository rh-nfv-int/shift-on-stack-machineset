apiVersion: k8s.cni.cncf.io/v1
kind: NetworkAttachmentDefinition
metadata:
  name: uplink2
spec:
  config: |-
    {
      "cniVersion": "0.3.1",
      "name": "uplink2",
      "type": "host-device",
      "pciBusId": "0000:00:05.0",
      "ipam": { }
    }
